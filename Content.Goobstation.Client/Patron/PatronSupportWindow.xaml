<ui:FancyWindow
    xmlns="https://spacestation14.io"
    xmlns:ui="clr-namespace:Content.Client.UserInterface.Controls;assembly=Content.Client"
    Title="{Loc 'discord-invite-window-title'}"
    MinSize="550 320"
    SetSize="550 320">
    <BoxContainer Orientation="Vertical" Margin="10" SeparationOverride="10">
        <PanelContainer StyleClasses="AngleRect" VerticalExpand="True" HorizontalExpand="True">
            <BoxContainer Orientation="Vertical" Margin="12" HorizontalExpand="True" VerticalAlignment="Center">
                <Label Text="{Loc 'discord-invite-window-header'}" StyleClasses="LabelHeadingBigger" HorizontalAlignment="Center" />
                <Control MinSize="0 8" />
                <RichTextLabel Text="{Loc 'discord-invite-window-description'}" HorizontalExpand="True" />
            </BoxContainer>
        </PanelContainer>

        <ui:StripeBack>
            <BoxContainer Orientation="Horizontal" HorizontalAlignment="Center" Margin="6 4">
                <Button Name="PatreonButton" Access="Public" Text="{Loc 'discord-invite-window-button'}" StyleClasses="ButtonBig" MinWidth="200" />
            </BoxContainer>
        </ui:StripeBack>
    </BoxContainer>
</ui:FancyWindow>
